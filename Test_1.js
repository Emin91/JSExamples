//Sum of two number from user with switch
var a = Number(prompt('type first num'))
var b = Number(prompt('type second num'))
var res = a + b;

switch (res) {
    case res:
        alert(`Result of ${a} + ${b} = ${res}`)
        break
    default: 
        alert('I don know your res')   
        break     
}